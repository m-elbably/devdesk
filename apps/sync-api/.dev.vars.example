# Copied to .dev.vars by `pnpm dev` so wrangler exposes these as worker bindings.
# The admin account is created on the first login attempt, only if no admin exists.
DEFAULT_ADMIN_USERNAME=admin@example.com
DEFAULT_ADMIN_PASSWORD=change-me-at-least-8-chars
